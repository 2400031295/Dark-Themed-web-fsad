services:
  - type: web
    name: assignment-hub-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:backend
    envVars:
      - key: PORT
        value: 4000
