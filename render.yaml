services:
  - type: web
    name: assignment-hub-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: PORT
        sync: false
